import React from "react";
import ReactDOM from "react-dom/client";
import QualityPortal from "./QualityPortal.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <QualityPortal />
  </React.StrictMode>
);
